import { background } from "./background";
import { characterText } from "./character-text";
import { container } from "./container";

export const helper = {
  container: container,
  background: background,
  characterText: characterText,
};
