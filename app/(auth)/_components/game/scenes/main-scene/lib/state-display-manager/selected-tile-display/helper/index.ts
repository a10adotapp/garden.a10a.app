import { background } from "./background";
import { container } from "./container";
import { positionText } from "./position-text";

export const helper = {
  container: container,
  background: background,
  positionText: positionText,
};
